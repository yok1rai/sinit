[package]
name = "sinit"
version = "0.0.3"
edition = "2024"

[dependencies]
nix = { version = "0.30", features = [
    "process",
    "user",
    "mount",
    "signal",
    "time",
    "fs"
] }
libc = "0.2"
